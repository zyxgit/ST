---
globs: Web/**/*
---

# ST Vue Web Rules

- Read `docs/frontend/README.md` and `docs/skills/frontend.md`.
- Use the shared request layer in `Web/src/lib/request.ts`.
- Keep routes, menus, and permission codes aligned with backend Identity.
- For frontend changes, prefer `cd Web && pnpm build`.
